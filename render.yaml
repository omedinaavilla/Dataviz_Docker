services:
  - type: web
    name: dataviz-docker
    env: docker
    plan: free
    branch: main
    repo: https://github.com/omedinaavilla/Dataviz_Docker
    dockerfilePath: ./Dockerfile
    autoDeploy: true